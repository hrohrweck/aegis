services:
  curator:
    build: .
    container_name: aegis
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      - CONFIG_PATH=config/config.yaml
